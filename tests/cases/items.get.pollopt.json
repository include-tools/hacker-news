{
  "name": "items.get returns a poll option with its parent poll and score",
  "tool": "items.get",
  "args": { "id": 160705 },
  "cassette": "items.get.pollopt",
  "expect": {
    "contains": ["\"type\":\"pollopt\"", "\"poll\":160704", "\"score\":335"],
    "calls": [
      { "method": "GET", "host": "hacker-news.firebaseio.com", "path": "/v0/item/160705.json", "headers": { "Authorization": "" } }
    ]
  }
}
