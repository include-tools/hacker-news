{
  "name": "users.get returns a profile only by default (no hydration)",
  "tool": "users.get",
  "args": { "username": "jl" },
  "cassette": "users.get.profile",
  "expect": {
    "contains": ["\"id\":\"jl\"", "\"karma\":4522", "\"submitted_count\":851", "\"truncated\":true", "\"recent_submissions\":[]", "Date("],
    "calls": [
      { "method": "GET", "host": "hacker-news.firebaseio.com", "path": "/v0/user/jl.json", "headers": { "Authorization": "" } }
    ]
  }
}
